let key = true;
let documents = true;
let pen = true;
let apple = false;
let orange = true;
let shouldGoToWork = 'shouldGoToWork'
if (key && documents && pen && (apple || orange)){
    console.log(shouldGoToWork)
}
